Closes #

### What changed

What this does, in the terms the issue is written in.

### What failure it prevents

The thing that could go wrong before this change and cannot afterwards. If the
change prevents nothing and is a cleanup, say that instead of inventing a
failure.

### The evidence

Every number and every claim about behaviour carries the command that produced
it, run at the commit being pushed rather than in a working tree. Paste the
command and its output.

```
```

### Means

Fill this in where this change introduces a language, a format, a tool, a runtime
or a dependency the tree does not already carry. Name the means and say why it
fits, including what it costs to add. Delete this section where nothing new is
introduced.

### What is not covered

What a reader might reasonably expect to find here and will not, and where it
lives instead. A condition on the issue that this change does not meet belongs
here by name, not left to be noticed.
